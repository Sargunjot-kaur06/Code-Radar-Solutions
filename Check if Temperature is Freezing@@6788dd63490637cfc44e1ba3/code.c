#include<stdio.h>
int main(){
    float a;
    scanf("%f", &a);
    if(a < 0)
    {
        printf("Freezing");
    }
    if else(a >= 0)
    {
        printf("Above Freezing");
    }
    return 0;
}