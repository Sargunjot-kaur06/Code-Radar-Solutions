#include<sdtio.h>
int main() {
    int n;
    printf("enter the number of elements: ");
    scanf("%d",&n);
    for(int i = 0; i < n; i++) {
        scanf("%d",a[i]);
    }
    max = a[0]; {
    if(a[i] > max)
    max = a[i];
    }
    printf("%d",max);

    min = a[0];
    {
        if(a[i] < min)
        min = a[i];
    }
    printf("%d",min);
    
}